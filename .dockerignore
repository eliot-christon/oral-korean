# Keep the build context small and the image free of host-only state. The Dockerfile
# copies what it needs by name, so anything missed here is excluded anyway.
.venv
.git
.github
.claude
.cache
.data

frontend/node_modules
frontend/dist

.mypy_cache
.pytest_cache
.ruff_cache
__pycache__
*.py[oc]
